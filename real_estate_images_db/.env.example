# Required for init_db.sh
DB_HOST=localhost
DB_PORT=5000
DB_NAME=myapp
DB_SUPERUSER=postgres
DB_SUPERUSER_PASSWORD= # optional if local ident auth
APP_DB_USER=appuser
APP_DB_PASSWORD=dbuser123

# Crypto: database master key (used by pgcrypto via gen_salt; for deterministic encryption use application layer)
# Never commit real keys; provide via secret management in production.
# If unset, pgcrypto defaults will be used.
CRYPTO_KDF_ITERATIONS=2048
```
